# Base URL of the Issue Tracker API (no trailing slash).
# Default dev setup: run the API on port 3001 so Next.js can use 3000.
# In backend/.env set: PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:3001
